    Physical Server(class Webserv)
                   |
    Port Server(class PortServer)
                   |
    Virtual Server Config(class VirtualServerConfig)
                   |
    Virtual Server Config Directory(class VirtualServerConfigDirectory)

ConfigFileParse:

// going through conf #1:
- for new ip:port pair create PortServer instance
- PortServer binding
- add Port Server to Webserv class

// going through conf #2:
- for new ServerName with ip:port pair create VirtualServer instance

// while Accept:
- add PortServer to client class

// while parsing host:
- searching compatible Virtual Server by host - serverName, else set default Virtual Server



----STATES----

REDIRECT

ERROR_REQUEST_NOT_VALID
ERROR_METHOD_NOT_ALLOWED
ERROR_PATH_NOT_AVAILABLE
ERROR_BODY_OVER_MAX_SIZE

GET_DIR
GET_AUTOINDEX
GET_REDIRECT
GET_FILE
GET_FILE__FILE_NOT_FOUND
GET_CGI

POST_FILE
POST_MULTI_PART
POST_CGI

DELETE_FILE

HEAD_FILE